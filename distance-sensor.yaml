# Board: Generic ESP32 Board (Generic)
# Definition: definitions/boards/generic-esp32/manifest.yaml

esphome:
  name: distance-sensor
  friendly_name: Distance Sensor

esp32:
  variant: esp32
  framework:
    type: esp-idf
logger:
  
api:
  encryption:
    key: !secret distance_sensor__encryption_key
  
ota:
  - platform: esphome

wifi:
  networks:
    - ssid: !secret campus_wifi_ssid
      eap:
        identity: !secret illinoisnet_eap_identity
        username: !secret illinoisnet_eap_username
        password: !secret illinoisnet_eap_password
  power_save_mode: NONE
        
        
  ap:
    ssid: Distance Sensor Fallback Hotspot
    password: !secret distance_sensor_fallback_ap_password
  
i2c:
  sda: GPIO33
  scl: GPIO25
  scan: true

sensor:
  - platform: vl53l0x
    name: Distance Sensor
    id: sensor_vl53l0x_1
    address: 0x29
    update_interval: 100ms
    filters: 
    unit_of_measurement: mm
    device_class: distance
    accuracy_decimals: 1
    long_range: true
  
    
  
captive_portal:
  
mdns:
  
  
